# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Surcharges locales : elles peuvent contenir des secrets (clé de service).
# `.env` lui-même reste versionné : Lovable y lit les variables VITE_* au
# build, et elles ne contiennent que des valeurs publishable déjà exposées
# dans le bundle client. Voir .env.example.
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
